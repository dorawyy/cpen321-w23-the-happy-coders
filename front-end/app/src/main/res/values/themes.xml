<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LangSync" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.LangSync" parent="Base.Theme.LangSync" />

    <style name="Button.Round" parent="Base.Theme.LangSync">
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeBottomLeft">16dp</item>
        <item name="cornerSizeBottomRight">16dp</item>
        <item name="cornerFamily">rounded</item>
        <item name="borderWidth">2dp</item>
        <item name="backgroundColor">@color/white</item>
    </style>

    <style name="BottomNavigation.ActiveTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/poppins_semibold</item>
    </style>

    <style name="BottomNavigation.InactiveTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/poppins_light</item>
    </style>

    <style name="Base.Theme.LangSync.AllowColor" parent="Theme.AppCompat.Light.NoActionBar"/>

</resources>